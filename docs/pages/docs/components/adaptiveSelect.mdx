import Adaptiveness from "@/shared/Adaptiveness";
import SourceLinks from "@/shared/sourceLinks";
import Warning from "@/shared/warning";
import { SourceCode, Tab, Tabs } from "rspress/theme";

# Adaptive Select

<Adaptiveness
  items={[
    "Desktop: doesn't block interaction with other page elements when open",
    "Android & iOS: uses the native device select",
    "iOS: native styling",
  ]}
/>

<Tabs>
  <Tab label="Adaptive">
    ```tsx
    import DocsTheme from "@/shared/docsTheme";
    import AdaptiveSelect, {
      AdaptiveSelectItem,
    } from "adaptive-material-ui/components/adaptiveSelect";

    export default function Basic() {
      return (
        <DocsTheme>
          <AdaptiveSelect defaultValue={1}>
            <AdaptiveSelectItem value={1}>One</AdaptiveSelectItem>
            <AdaptiveSelectItem value={2}>Two</AdaptiveSelectItem>
          </AdaptiveSelect>
        </DocsTheme>
      );
    }

    ```

  </Tab>
  <Tab label="iOS">
    ```tsx
    import DocsTheme from "@/shared/docsTheme";
    import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
    import AdaptiveSelect, {
      AdaptiveSelectItem,
    } from "adaptive-material-ui/components/adaptiveSelect";

    export default function Basic() {
      return (
        <DocsTheme>
          <AdaptiveSelect adaptiveMode={AdaptiveMode.ios} defaultValue={1}>
            <AdaptiveSelectItem value={1}>One</AdaptiveSelectItem>
            <AdaptiveSelectItem value={2}>Two</AdaptiveSelectItem>
          </AdaptiveSelect>
        </DocsTheme>
      );
    }
    ```
    <Warning>This select uses the native device select so will behave differently depending on the device</Warning>

  </Tab>
  <Tab label="Android">
    ```tsx
    import DocsTheme from "@/shared/docsTheme";
    import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
    import AdaptiveSelect, {
      AdaptiveSelectItem,
    } from "adaptive-material-ui/components/adaptiveSelect";

    export default function Basic() {
      return (
        <DocsTheme>
          <AdaptiveSelect adaptiveMode={AdaptiveMode.android} defaultValue={1}>
            <AdaptiveSelectItem value={1}>One</AdaptiveSelectItem>
            <AdaptiveSelectItem value={2}>Two</AdaptiveSelectItem>
          </AdaptiveSelect>
        </DocsTheme>
      );
    }
    ```
    <Warning>This select uses the native device select so will behave differently depending on the device</Warning>

  </Tab>
  <Tab label="Desktop">
    ```tsx
    import DocsTheme from "@/shared/docsTheme";
    import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
    import AdaptiveSelect, {
      AdaptiveSelectItem,
    } from "adaptive-material-ui/components/adaptiveSelect";

    export default function Basic() {
      return (
        <DocsTheme>
          <AdaptiveSelect adaptiveMode={AdaptiveMode.desktop} defaultValue={1}>
            <AdaptiveSelectItem value={1}>One</AdaptiveSelectItem>
            <AdaptiveSelectItem value={2}>Two</AdaptiveSelectItem>
          </AdaptiveSelect>
        </DocsTheme>
      );
    }
    ```

  </Tab>
</Tabs>

## Customization

The iOS mode can be customized using the iOS class or theme as shown below. The other modes use the standard [Material UI Select](https://mui.com/material-ui/react-select/#customization).

### iOS Custom Class

```tsx
import DocsTheme from "@/shared/docsTheme";
import { styled } from "@mui/material/styles";
import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
import AdaptiveSelect, {
  AdaptiveSelectItem,
} from "adaptive-material-ui/components/adaptiveSelect";

const AdaptiveSelectStyled = styled(AdaptiveSelect)({
  "&.ios": {},
});

export default function Customization_Classes() {
  return (
    <DocsTheme>
      <AdaptiveSelect
        adaptiveMode={AdaptiveMode.ios}
        classes={{ ios: "ios" }}
        defaultValue={1}
      >
        <AdaptiveSelectItem value={1}>One</AdaptiveSelectItem>
        <AdaptiveSelectItem value={2}>Two</AdaptiveSelectItem>
      </AdaptiveSelect>
    </DocsTheme>
  );
}
```

### iOS Custom Theme

```tsx
import DocsTheme from "@/shared/docsTheme";
import { createTheme, ThemeProvider } from "@mui/material/styles";
import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
import AdaptiveSelect, {
  AdaptiveSelectItem,
} from "adaptive-material-ui/components/adaptiveSelect";
import type {} from "adaptive-material-ui/themeAugmentation";

function customizeTheme(theme: Theme) {
  return createTheme({
    ...theme,
    components: {
      ...theme.components,
      AdaptiveSelect: {
        styleOverrides: {
          ios: {},
        },
      },
    },
  });
}

export default function Customization_Theme() {
  return (
    <DocsTheme>
      <ThemeProvider theme={customizeTheme}>
        <AdaptiveSelect adaptiveMode={AdaptiveMode.ios} defaultValue={1}>
          <AdaptiveSelectItem value={1}>One</AdaptiveSelectItem>
          <AdaptiveSelectItem value={2}>Two</AdaptiveSelectItem>
        </AdaptiveSelect>
      </ThemeProvider>
    </DocsTheme>
  );
}
```

<SourceLinks
  codeUrl="components/adaptiveSelect.tsx"
  docsUrl="components/adaptiveSelect.mdx"
/>
