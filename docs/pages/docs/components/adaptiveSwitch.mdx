import Links from "@/shared/links";
import { Tab, Tabs } from "rspress/theme";

# Adaptive Switch

## Adaptiveness

- iOS: native styling

<Tabs>
  <Tab label="Adaptive">
    ```tsx
    import AdaptiveSwitch from "adaptive-material-ui/components/switch/adaptiveSwitch";

    export default function Basic() {
      return <AdaptiveSwitch defaultChecked />;
    }
    ```

  </Tab>
  <Tab label="iOS">
    ```tsx
    import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
    import AdaptiveSwitch from "adaptive-material-ui/components/switch/adaptiveSwitch";

    export default function Basic() {
      return <AdaptiveSwitch adaptiveMode={AdaptiveMode.ios} defaultChecked />;
    }
    ```

  </Tab>
  <Tab label="Android">
    ```tsx
    import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
    import AdaptiveSwitch from "adaptive-material-ui/components/switch/adaptiveSwitch";

    export default function Basic() {
      return <AdaptiveSwitch adaptiveMode={AdaptiveMode.android} defaultChecked />;
    }
    ```

  </Tab>
  <Tab label="Desktop">
    ```tsx
    import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
    import AdaptiveSwitch from "adaptive-material-ui/components/switch/adaptiveSwitch";

    export default function Basic() {
      return <AdaptiveSwitch adaptiveMode={AdaptiveMode.desktop} defaultChecked />;
    }
    ```

  </Tab>
</Tabs>

## iOS Variants

```tsx
import FormControlLabel from "@mui/material/FormControlLabel";
import Grid from "@mui/material/Grid";
import { AdaptiveMode } from "adaptive-material-ui/adaptiveMode";
import AdaptiveSwitch from "adaptive-material-ui/components/switch/adaptiveSwitch";

export default function Basic() {
  return (
    <Grid container direction="row" spacing={2}>
      <Grid>
        <FormControlLabel
          control={
            <AdaptiveSwitch
              adaptiveMode={AdaptiveMode.ios}
              color="secondary"
              defaultChecked
            />
          }
          label="Secondary Color"
        />
      </Grid>
      <Grid>
        <FormControlLabel
          control={
            <AdaptiveSwitch
              adaptiveMode={AdaptiveMode.ios}
              defaultChecked
              disabled
            />
          }
          label="Disabled"
        />
      </Grid>
      <Grid>
        <FormControlLabel
          control={
            <AdaptiveSwitch
              adaptiveMode={AdaptiveMode.ios}
              defaultChecked
              size="small"
            />
          }
          label="Small"
        />
      </Grid>
    </Grid>
  );
}
```

## Links

<Links
  codeUrl="components/adaptiveSwitch.tsx"
  docsUrl="components/adaptiveSwitch.mdx"
  mui={{ title: "Switch", url: "react-switch" }}
/>
